repositories:
  - name: stable
    url: https://charts.helm.sh/stable

helmDefaults:
  atomic: true
  timeout: 300

environments:
  production:
    values:
      []
---
releases:
  - name: {{ .Release.Name }}
    namespace: default
    chart: ./
    values:
      - values.yaml