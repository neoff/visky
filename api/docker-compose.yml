version: "3.7"
services:
#   mongo:
#     container_name: mongo
#     image: mongo
#     restart: always
#     volumes:
#       - ./data:/data/db
#     ports:
#       - 27017:27017
  app:
    platform: linux/amd64
    container_name: varg/vk-frisky-parser
    restart: always
    build:
      context: .
      dockerfile: Dockerfile.dev
    env_file: .env
    environment:
      - PORT=${PORT}
#      - MONGO_URL=${MONGO_URL}
    ports:
      - 3000:3000
#    external_links:
#      - mongo
#    depends_on:
#      - mongo
volumes:
  mongo-data:
    driver: local