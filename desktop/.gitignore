node_modules/

# Generated by scripts/build-desktop.sh, all of it reproducible:
#   web/          the Expo bundle exported for platform web
#   dist/         the .app, .dmg and .pkg
#   shell/target/ the Rust build cache — one directory per architecture ever
#                 built, which is why artifacts are copied out to dist/
#   shell/gen/    the ACL schemas tauri-build regenerates on every build
web/
dist/
shell/target/
shell/gen/
