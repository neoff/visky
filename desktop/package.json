{
  "name": "visky-desktop",
  "version": "1.0.0",
  "private": true,
  "description": "visky desktop player — the Expo web bundle in a Tauri shell",
  "scripts": {
    "start": "tauri dev --no-watch --config shell/tauri.conf.json",
    "dist": "../scripts/build-desktop.sh"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
