diff --git a/node_modules/react-native-track-player/android/src/main/java/com/doublesymmetry/trackplayer/module/MusicModule.kt b/node_modules/react-native-track-player/android/src/main/java/com/doublesymmetry/trackplayer/module/MusicModule.kt
index b2409a0..ed1b1b3 100644
--- a/node_modules/react-native-track-player/android/src/main/java/com/doublesymmetry/trackplayer/module/MusicModule.kt
+++ b/node_modules/react-native-track-player/android/src/main/java/com/doublesymmetry/trackplayer/module/MusicModule.kt
@@ -1,6 +1,7 @@
 package com.doublesymmetry.trackplayer.module
 
 import android.content.*
+import android.app.ActivityManager
 import android.os.Build
 import android.os.Bundle
 import android.os.IBinder
@@ -19,12 +20,16 @@ import com.facebook.react.bridge.*
 import com.google.android.exoplayer2.DefaultLoadControl.*
 import com.google.android.exoplayer2.Player
 import kotlinx.coroutines.MainScope
+import kotlinx.coroutines.CoroutineScope
 import kotlinx.coroutines.delay
 import kotlinx.coroutines.launch
 import timber.log.Timber
 import java.util.*
 import javax.annotation.Nonnull
 
+private fun CoroutineScope.launchReact(block: suspend CoroutineScope.() -> Unit) {
+    launch(block = block)
+}
 
 /**
  * @author Milen Pivchev @mpivchev
@@ -159,6 +164,19 @@ class MusicModule(reactContext: ReactApplicationContext) : ReactContextBaseJavaM
         }
     }
 
+    /**
+     * PATCHED (visky): is this process foreground by the platform's own reckoning?
+     *
+     * IMPORTANCE_FOREGROUND_SERVICE (125) or better is precisely the condition
+     * under which starting a foreground service is permitted, so it is the
+     * honest test for the crash the guard above exists to prevent.
+     */
+    private fun isProcessForeground(): Boolean {
+        val state = ActivityManager.RunningAppProcessInfo()
+        ActivityManager.getMyMemoryState(state)
+        return state.importance <= ActivityManager.RunningAppProcessInfo.IMPORTANCE_FOREGROUND_SERVICE
+    }
+
     @ReactMethod
     fun setupPlayer(data: ReadableMap?, promise: Promise) {
         if (isServiceBound) {
@@ -170,7 +188,25 @@ class MusicModule(reactContext: ReactApplicationContext) : ReactContextBaseJavaM
         }
 
         // prevent crash Fatal Exception: android.app.RemoteServiceException$ForegroundServiceDidNotStartInTimeException
-        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O && AppForegroundTracker.backgrounded) {
+        //
+        // PATCHED (visky): the original guard is `AppForegroundTracker.backgrounded`
+        // alone, and that tracker counts ACTIVITIES -- it observes
+        // ProcessLifecycleOwner's resume/pause and nothing else. A process that is
+        // already running a foreground service therefore still reads as
+        // backgrounded, and setupPlayer is refused.
+        //
+        // That case is real on Android Automotive: the car owns the foreground,
+        // our browse service is started from a cold boot to answer a tap, and no
+        // Activity of ours will ever exist. CarBrowserService puts itself in the
+        // foreground first (the car grants a short FGS allowlist when it
+        // dispatches the tap), so by the time this runs, starting another
+        // foreground service is legal and the exception this guard prevents
+        // cannot occur -- which is exactly what the importance check below
+        // establishes.
+        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O &&
+            AppForegroundTracker.backgrounded &&
+            !isProcessForeground()
+        ) {
             promise.reject(
                 "android_cannot_setup_player_in_background",
                 "On Android the app must be in the foreground when setting up the player."
@@ -251,8 +287,8 @@ class MusicModule(reactContext: ReactApplicationContext) : ReactContextBaseJavaM
     }
 
     @ReactMethod
-    fun updateOptions(data: ReadableMap?, callback: Promise) = scope.launch {
-        if (verifyServiceBoundOrReject(callback)) return@launch
+    fun updateOptions(data: ReadableMap?, callback: Promise) = scope.launchReact {
+        if (verifyServiceBoundOrReject(callback)) return@launchReact
 
         val options = Arguments.toBundle(data)
 
@@ -264,14 +300,14 @@ class MusicModule(reactContext: ReactApplicationContext) : ReactContextBaseJavaM
     }
 
     @ReactMethod
-    fun add(data: ReadableArray?, insertBeforeIndex: Int, callback: Promise) = scope.launch {
-        if (verifyServiceBoundOrReject(callback)) return@launch
+    fun add(data: ReadableArray?, insertBeforeIndex: Int, callback: Promise) = scope.launchReact {
+        if (verifyServiceBoundOrReject(callback)) return@launchReact
 
         try {
             val tracks = readableArrayToTrackList(data);
             if (insertBeforeIndex < -1 || insertBeforeIndex > musicService.tracks.size) {
                 callback.reject("index_out_of_bounds", "The track index is out of bounds")
-                return@launch
+                return@launchReact
             }
             val index = if (insertBeforeIndex == -1) musicService.tracks.size else insertBeforeIndex
             musicService.add(
@@ -285,11 +321,11 @@ class MusicModule(reactContext: ReactApplicationContext) : ReactContextBaseJavaM
     }
 
     @ReactMethod
-    fun load(data: ReadableMap?, callback: Promise) = scope.launch {
-        if (verifyServiceBoundOrReject(callback)) return@launch
+    fun load(data: ReadableMap?, callback: Promise) = scope.launchReact {
+        if (verifyServiceBoundOrReject(callback)) return@launchReact
         if (data == null) {
             callback.resolve(null)
-            return@launch
+            return@launchReact
         }
         val bundle = Arguments.toBundle(data);
         if (bundle is Bundle) {
@@ -301,15 +337,15 @@ class MusicModule(reactContext: ReactApplicationContext) : ReactContextBaseJavaM
     }
 
     @ReactMethod
-    fun move(fromIndex: Int, toIndex: Int, callback: Promise) = scope.launch {
-        if (verifyServiceBoundOrReject(callback)) return@launch
+    fun move(fromIndex: Int, toIndex: Int, callback: Promise) = scope.launchReact {
+        if (verifyServiceBoundOrReject(callback)) return@launchReact
         musicService.move(fromIndex, toIndex)
         callback.resolve(null)
     }
 
     @ReactMethod
-    fun remove(data: ReadableArray?, callback: Promise) = scope.launch {
-        if (verifyServiceBoundOrReject(callback)) return@launch
+    fun remove(data: ReadableArray?, callback: Promise) = scope.launchReact {
+        if (verifyServiceBoundOrReject(callback)) return@launchReact
         val inputIndexes = Arguments.toList(data)
         if (inputIndexes != null) {
             val size = musicService.tracks.size
@@ -321,7 +357,7 @@ class MusicModule(reactContext: ReactApplicationContext) : ReactContextBaseJavaM
                         "index_out_of_bounds",
                         "One or more indexes was out of bounds"
                     )
-                    return@launch
+                    return@launchReact
                 }
                 indexes.add(index)
             }
@@ -332,8 +368,8 @@ class MusicModule(reactContext: ReactApplicationContext) : ReactContextBaseJavaM
 
     @ReactMethod
     fun updateMetadataForTrack(index: Int, map: ReadableMap?, callback: Promise) =
-        scope.launch {
-            if (verifyServiceBoundOrReject(callback)) return@launch
+        scope.launchReact {
+            if (verifyServiceBoundOrReject(callback)) return@launchReact
 
             if (index < 0 || index >= musicService.tracks.size) {
                 callback.reject("index_out_of_bounds", "The index is out of bounds")
@@ -348,8 +384,8 @@ class MusicModule(reactContext: ReactApplicationContext) : ReactContextBaseJavaM
         }
 
     @ReactMethod
-    fun updateNowPlayingMetadata(map: ReadableMap?, callback: Promise) = scope.launch {
-        if (verifyServiceBoundOrReject(callback)) return@launch
+    fun updateNowPlayingMetadata(map: ReadableMap?, callback: Promise) = scope.launchReact {
+        if (verifyServiceBoundOrReject(callback)) return@launchReact
 
         if (musicService.tracks.isEmpty())
             callback.reject("no_current_item", "There is no current item in the player")
@@ -364,8 +400,8 @@ class MusicModule(reactContext: ReactApplicationContext) : ReactContextBaseJavaM
     }
 
     @ReactMethod
-    fun clearNowPlayingMetadata(callback: Promise) = scope.launch {
-        if (verifyServiceBoundOrReject(callback)) return@launch
+    fun clearNowPlayingMetadata(callback: Promise) = scope.launchReact {
+        if (verifyServiceBoundOrReject(callback)) return@launchReact
 
         if (musicService.tracks.isEmpty())
             callback.reject("no_current_item", "There is no current item in the player")
@@ -375,16 +411,16 @@ class MusicModule(reactContext: ReactApplicationContext) : ReactContextBaseJavaM
     }
 
     @ReactMethod
-    fun removeUpcomingTracks(callback: Promise) = scope.launch {
-        if (verifyServiceBoundOrReject(callback)) return@launch
+    fun removeUpcomingTracks(callback: Promise) = scope.launchReact {
+        if (verifyServiceBoundOrReject(callback)) return@launchReact
 
         musicService.removeUpcomingTracks()
         callback.resolve(null)
     }
 
     @ReactMethod
-    fun skip(index: Int, initialTime: Float, callback: Promise) = scope.launch {
-        if (verifyServiceBoundOrReject(callback)) return@launch
+    fun skip(index: Int, initialTime: Float, callback: Promise) = scope.launchReact {
+        if (verifyServiceBoundOrReject(callback)) return@launchReact
 
         musicService.skip(index)
 
@@ -396,8 +432,8 @@ class MusicModule(reactContext: ReactApplicationContext) : ReactContextBaseJavaM
     }
 
     @ReactMethod
-    fun skipToNext(initialTime: Float, callback: Promise) = scope.launch {
-        if (verifyServiceBoundOrReject(callback)) return@launch
+    fun skipToNext(initialTime: Float, callback: Promise) = scope.launchReact {
+        if (verifyServiceBoundOrReject(callback)) return@launchReact
 
         musicService.skipToNext()
 
@@ -409,8 +445,8 @@ class MusicModule(reactContext: ReactApplicationContext) : ReactContextBaseJavaM
     }
 
     @ReactMethod
-    fun skipToPrevious(initialTime: Float, callback: Promise) = scope.launch {
-        if (verifyServiceBoundOrReject(callback)) return@launch
+    fun skipToPrevious(initialTime: Float, callback: Promise) = scope.launchReact {
+        if (verifyServiceBoundOrReject(callback)) return@launchReact
 
         musicService.skipToPrevious()
 
@@ -422,8 +458,8 @@ class MusicModule(reactContext: ReactApplicationContext) : ReactContextBaseJavaM
     }
 
     @ReactMethod
-    fun reset(callback: Promise) = scope.launch {
-        if (verifyServiceBoundOrReject(callback)) return@launch
+    fun reset(callback: Promise) = scope.launchReact {
+        if (verifyServiceBoundOrReject(callback)) return@launchReact
 
         musicService.stop()
         delay(300) // Allow playback to stop
@@ -433,134 +469,134 @@ class MusicModule(reactContext: ReactApplicationContext) : ReactContextBaseJavaM
     }
 
     @ReactMethod
-    fun play(callback: Promise) = scope.launch {
-        if (verifyServiceBoundOrReject(callback)) return@launch
+    fun play(callback: Promise) = scope.launchReact {
+        if (verifyServiceBoundOrReject(callback)) return@launchReact
 
         musicService.play()
         callback.resolve(null)
     }
 
     @ReactMethod
-    fun pause(callback: Promise) = scope.launch {
-        if (verifyServiceBoundOrReject(callback)) return@launch
+    fun pause(callback: Promise) = scope.launchReact {
+        if (verifyServiceBoundOrReject(callback)) return@launchReact
 
         musicService.pause()
         callback.resolve(null)
     }
 
     @ReactMethod
-    fun stop(callback: Promise) = scope.launch {
-        if (verifyServiceBoundOrReject(callback)) return@launch
+    fun stop(callback: Promise) = scope.launchReact {
+        if (verifyServiceBoundOrReject(callback)) return@launchReact
 
         musicService.stop()
         callback.resolve(null)
     }
 
     @ReactMethod
-    fun seekTo(seconds: Float, callback: Promise) = scope.launch {
-        if (verifyServiceBoundOrReject(callback)) return@launch
+    fun seekTo(seconds: Float, callback: Promise) = scope.launchReact {
+        if (verifyServiceBoundOrReject(callback)) return@launchReact
 
         musicService.seekTo(seconds)
         callback.resolve(null)
     }
 
     @ReactMethod
-    fun seekBy(offset: Float, callback: Promise) = scope.launch {
-        if (verifyServiceBoundOrReject(callback)) return@launch
+    fun seekBy(offset: Float, callback: Promise) = scope.launchReact {
+        if (verifyServiceBoundOrReject(callback)) return@launchReact
 
         musicService.seekBy(offset)
         callback.resolve(null)
     }
 
     @ReactMethod
-    fun retry(callback: Promise) = scope.launch {
-        if (verifyServiceBoundOrReject(callback)) return@launch
+    fun retry(callback: Promise) = scope.launchReact {
+        if (verifyServiceBoundOrReject(callback)) return@launchReact
 
         musicService.retry()
         callback.resolve(null)
     }
 
     @ReactMethod
-    fun setVolume(volume: Float, callback: Promise) = scope.launch {
-        if (verifyServiceBoundOrReject(callback)) return@launch
+    fun setVolume(volume: Float, callback: Promise) = scope.launchReact {
+        if (verifyServiceBoundOrReject(callback)) return@launchReact
 
         musicService.setVolume(volume)
         callback.resolve(null)
     }
 
     @ReactMethod
-    fun getVolume(callback: Promise) = scope.launch {
-        if (verifyServiceBoundOrReject(callback)) return@launch
+    fun getVolume(callback: Promise) = scope.launchReact {
+        if (verifyServiceBoundOrReject(callback)) return@launchReact
 
         callback.resolve(musicService.getVolume())
     }
 
     @ReactMethod
-    fun setRate(rate: Float, callback: Promise) = scope.launch {
-        if (verifyServiceBoundOrReject(callback)) return@launch
+    fun setRate(rate: Float, callback: Promise) = scope.launchReact {
+        if (verifyServiceBoundOrReject(callback)) return@launchReact
 
         musicService.setRate(rate)
         callback.resolve(null)
     }
 
     @ReactMethod
-    fun getRate(callback: Promise) = scope.launch {
-        if (verifyServiceBoundOrReject(callback)) return@launch
+    fun getRate(callback: Promise) = scope.launchReact {
+        if (verifyServiceBoundOrReject(callback)) return@launchReact
 
         callback.resolve(musicService.getRate())
     }
 
     @ReactMethod
-    fun setRepeatMode(mode: Int, callback: Promise) = scope.launch {
-        if (verifyServiceBoundOrReject(callback)) return@launch
+    fun setRepeatMode(mode: Int, callback: Promise) = scope.launchReact {
+        if (verifyServiceBoundOrReject(callback)) return@launchReact
 
         musicService.setRepeatMode(RepeatMode.fromOrdinal(mode))
         callback.resolve(null)
     }
 
     @ReactMethod
-    fun getRepeatMode(callback: Promise) = scope.launch {
-        if (verifyServiceBoundOrReject(callback)) return@launch
+    fun getRepeatMode(callback: Promise) = scope.launchReact {
+        if (verifyServiceBoundOrReject(callback)) return@launchReact
 
         callback.resolve(musicService.getRepeatMode().ordinal)
     }
 
     @ReactMethod
-    fun setPlayWhenReady(playWhenReady: Boolean, callback: Promise) = scope.launch {
-        if (verifyServiceBoundOrReject(callback)) return@launch
+    fun setPlayWhenReady(playWhenReady: Boolean, callback: Promise) = scope.launchReact {
+        if (verifyServiceBoundOrReject(callback)) return@launchReact
 
         musicService.playWhenReady = playWhenReady
         callback.resolve(null)
     }
 
     @ReactMethod
-    fun getPlayWhenReady(callback: Promise) = scope.launch {
-        if (verifyServiceBoundOrReject(callback)) return@launch
+    fun getPlayWhenReady(callback: Promise) = scope.launchReact {
+        if (verifyServiceBoundOrReject(callback)) return@launchReact
 
         callback.resolve(musicService.playWhenReady)
     }
 
     @ReactMethod
-    fun getTrack(index: Int, callback: Promise) = scope.launch {
-        if (verifyServiceBoundOrReject(callback)) return@launch
+    fun getTrack(index: Int, callback: Promise) = scope.launchReact {
+        if (verifyServiceBoundOrReject(callback)) return@launchReact
 
         if (index >= 0 && index < musicService.tracks.size) {
-            callback.resolve(Arguments.fromBundle(musicService.tracks[index].originalItem))
+            callback.resolve(musicService.tracks[index].originalItem?.let { Arguments.fromBundle(it) })
         } else {
             callback.resolve(null)
         }
     }
 
     @ReactMethod
-    fun getQueue(callback: Promise) = scope.launch {
-        if (verifyServiceBoundOrReject(callback)) return@launch
+    fun getQueue(callback: Promise) = scope.launchReact {
+        if (verifyServiceBoundOrReject(callback)) return@launchReact
 
         callback.resolve(Arguments.fromList(musicService.tracks.map { it.originalItem }))
     }
 
     @ReactMethod
-    fun setQueue(data: ReadableArray?, callback: Promise) = scope.launch {
-        if (verifyServiceBoundOrReject(callback)) return@launch
+    fun setQueue(data: ReadableArray?, callback: Promise) = scope.launchReact {
+        if (verifyServiceBoundOrReject(callback)) return@launchReact
 
         try {
             musicService.clear()
@@ -572,48 +608,48 @@ class MusicModule(reactContext: ReactApplicationContext) : ReactContextBaseJavaM
     }
 
     @ReactMethod
-    fun getActiveTrackIndex(callback: Promise) = scope.launch {
-        if (verifyServiceBoundOrReject(callback)) return@launch
+    fun getActiveTrackIndex(callback: Promise) = scope.launchReact {
+        if (verifyServiceBoundOrReject(callback)) return@launchReact
         callback.resolve(
             if (musicService.tracks.isEmpty()) null else musicService.getCurrentTrackIndex()
         )
     }
 
     @ReactMethod
-    fun getActiveTrack(callback: Promise) = scope.launch {
-        if (verifyServiceBoundOrReject(callback)) return@launch
+    fun getActiveTrack(callback: Promise) = scope.launchReact {
+        if (verifyServiceBoundOrReject(callback)) return@launchReact
         callback.resolve(
             if (musicService.tracks.isEmpty()) null
-            else Arguments.fromBundle(
-                musicService.tracks[musicService.getCurrentTrackIndex()].originalItem
-            )
+            else musicService.tracks[musicService.getCurrentTrackIndex()].originalItem?.let {
+                Arguments.fromBundle(it)
+            }
         )
     }
 
     @ReactMethod
-    fun getDuration(callback: Promise) = scope.launch {
-        if (verifyServiceBoundOrReject(callback)) return@launch
+    fun getDuration(callback: Promise) = scope.launchReact {
+        if (verifyServiceBoundOrReject(callback)) return@launchReact
 
         callback.resolve(musicService.getDurationInSeconds())
     }
 
     @ReactMethod
-    fun getBufferedPosition(callback: Promise) = scope.launch {
-        if (verifyServiceBoundOrReject(callback)) return@launch
+    fun getBufferedPosition(callback: Promise) = scope.launchReact {
+        if (verifyServiceBoundOrReject(callback)) return@launchReact
 
         callback.resolve(musicService.getBufferedPositionInSeconds())
     }
 
     @ReactMethod
-    fun getPosition(callback: Promise) = scope.launch {
-        if (verifyServiceBoundOrReject(callback)) return@launch
+    fun getPosition(callback: Promise) = scope.launchReact {
+        if (verifyServiceBoundOrReject(callback)) return@launchReact
 
         callback.resolve(musicService.getPositionInSeconds())
     }
 
     @ReactMethod
-    fun getProgress(callback: Promise) = scope.launch {
-        if (verifyServiceBoundOrReject(callback)) return@launch
+    fun getProgress(callback: Promise) = scope.launchReact {
+        if (verifyServiceBoundOrReject(callback)) return@launchReact
         var bundle = Bundle()
         bundle.putDouble("duration", musicService.getDurationInSeconds());
         bundle.putDouble("position", musicService.getPositionInSeconds());
@@ -622,8 +658,8 @@ class MusicModule(reactContext: ReactApplicationContext) : ReactContextBaseJavaM
     }
 
     @ReactMethod
-    fun getPlaybackState(callback: Promise) = scope.launch {
-        if (verifyServiceBoundOrReject(callback)) return@launch
+    fun getPlaybackState(callback: Promise) = scope.launchReact {
+        if (verifyServiceBoundOrReject(callback)) return@launchReact
         callback.resolve(Arguments.fromBundle(musicService.getPlayerStateBundle(musicService.state)))
     }
 }
diff --git a/node_modules/react-native-track-player/android/src/main/java/com/doublesymmetry/trackplayer/service/MusicService.kt b/node_modules/react-native-track-player/android/src/main/java/com/doublesymmetry/trackplayer/service/MusicService.kt
index afa6b0f..c417e7b 100644
--- a/node_modules/react-native-track-player/android/src/main/java/com/doublesymmetry/trackplayer/service/MusicService.kt
+++ b/node_modules/react-native-track-player/android/src/main/java/com/doublesymmetry/trackplayer/service/MusicService.kt
@@ -741,7 +741,7 @@ class MusicService : HeadlessJsTaskService() {
 
     @MainThread
     private fun emit(event: String, data: Bundle? = null) {
-        reactNativeHost.reactInstanceManager.currentReactContext
+        reactContext
             ?.getJSModule(DeviceEventManagerModule.RCTDeviceEventEmitter::class.java)
             ?.emit(event, data?.let { Arguments.fromBundle(it) })
     }
@@ -751,7 +751,7 @@ class MusicService : HeadlessJsTaskService() {
         val payload = Arguments.createArray()
         data.forEach { payload.pushMap(Arguments.fromBundle(it)) }
 
-        reactNativeHost.reactInstanceManager.currentReactContext
+        reactContext
             ?.getJSModule(DeviceEventManagerModule.RCTDeviceEventEmitter::class.java)
             ?.emit(event, payload)
     }
diff --git a/node_modules/react-native-track-player/lib/web/TrackPlayer/Player.js b/node_modules/react-native-track-player/lib/web/TrackPlayer/Player.js
index 5953276..821a57c 100644
--- a/node_modules/react-native-track-player/lib/web/TrackPlayer/Player.js
+++ b/node_modules/react-native-track-player/lib/web/TrackPlayer/Player.js
@@ -7,6 +7,8 @@ export class Player {
     _current = undefined;
     _playWhenReady = false;
     _state = { state: State.None };
+    /** PATCHED. The shaka PreloadManager parked for the next track, if any. */
+    preloaded;
     // current getter/setter
     get current() {
         return this._current;
@@ -75,6 +77,12 @@ export class Player {
         // Attach player to the window to make it easy to access in the JS console.
         // @ts-ignore
         window.rntp = this.player;
+        // PATCHED. The wrapper this package exports has a fixed set of
+        // functions and no way to reach a method it does not know about, so the
+        // gapless hand-over below is reached through here. Web only, and only
+        // ever called by services/prefetch.
+        // @ts-ignore
+        window.rntpPlayer = this;
         this.hasInitialized = true;
     }
     /**
@@ -99,9 +107,70 @@ export class Player {
     /**
      * player control
      */
+    /**
+     * PATCHED. Prepare the NEXT track without disturbing the one playing.
+     *
+     * shaka's PreloadManager fetches the manifest and the head of the stream
+     * into a parked object that `load` can then adopt, which is the difference
+     * between a hand-over that stalls for a manifest fetch, a TLS handshake and
+     * the first segments, and one that does not. Without it every track change
+     * on the web build is an audible gap — the native players buffer the next
+     * queue entry themselves, this one had nothing.
+     *
+     * Idempotent per url, and deliberately quiet: a warm-up that fails costs
+     * nothing but the gap it was meant to close.
+     */
+    async preload(url) {
+        if (!this.player || !url || typeof this.player.preload !== 'function')
+            return;
+        if (this.preloaded?.url === url)
+            return;
+        this.destroyPreload();
+        try {
+            const manager = await this.player.preload(url);
+            if (!manager)
+                return;
+            // `load` may have run while we were on the network; it clears this,
+            // and adopting a manager after that would fight the track playing.
+            if (this.preloaded === undefined)
+                this.preloaded = { url, manager };
+            else
+                manager.destroy?.().catch(() => undefined);
+        }
+        catch (error) {
+            console.debug('==rntp-web: could not preload', error);
+        }
+    }
+    /** PATCHED. Drop a parked preload; it holds buffers. */
+    destroyPreload() {
+        const held = this.preloaded;
+        this.preloaded = undefined;
+        held?.manager?.destroy?.().catch(() => undefined);
+    }
     async load(track) {
         if (!this.player)
             throw new SetupNotCalledError();
+        // PATCHED. Adopt the parked preload when it is this track, so the
+        // hand-over is a swap rather than a fetch. Anything else is stale by
+        // definition — the queue moved somewhere we did not predict — and is
+        // destroyed rather than kept, because a PreloadManager holds buffers.
+        const held = this.preloaded;
+        this.preloaded = undefined;
+        if (held && held.url === track.url) {
+            try {
+                await this.player.load(held.manager);
+                this.current = track;
+                return;
+            }
+            catch (error) {
+                // A consumed or expired manager is not a reason to give up on
+                // the track: fall through and load it the ordinary way.
+                console.debug('==rntp-web: preloaded hand-over failed', error);
+            }
+        }
+        else {
+            held?.manager?.destroy?.().catch(() => undefined);
+        }
         await this.player.load(track.url);
         this.current = track;
     }
@@ -113,6 +182,7 @@ export class Player {
     async stop() {
         if (!this.player)
             throw new SetupNotCalledError();
+        this.destroyPreload();
         this.current = undefined;
         await this.player.unload();
     }
diff --git a/node_modules/react-native-track-player/lib/web/TrackPlayer/PlaylistPlayer.js b/node_modules/react-native-track-player/lib/web/TrackPlayer/PlaylistPlayer.js
index 67b34a4..d8426c2 100644
--- a/node_modules/react-native-track-player/lib/web/TrackPlayer/PlaylistPlayer.js
+++ b/node_modules/react-native-track-player/lib/web/TrackPlayer/PlaylistPlayer.js
@@ -4,6 +4,8 @@ import { State } from '../../src';
 export class PlaylistPlayer extends Player {
     // TODO: use immer to make the `playlist` immutable
     playlist = [];
+    /** PATCHED. Set by TrackPlayerModule; reports a hand-over that failed. */
+    onNextTrackFailed;
     lastIndex;
     _currentIndex;
     repeatMode = RepeatMode.Off;
@@ -34,7 +36,18 @@ export class PlaylistPlayer extends Player {
                 }
                 catch (err) {
                     if (err.message !== 'playlist_exhausted') {
-                        throw err;
+                        // PATCHED. This used to rethrow, out of a media element
+                        // event handler, where nothing awaits it: the track
+                        // ended, the next one failed to load and the only trace
+                        // was an unhandled rejection. Reported instead, so the
+                        // failure is attached to the track it belongs to — and
+                        // handed to the app, which can re-sign the url and pick
+                        // the show back up (services/recovery). Swallowing it
+                        // here is what made the player stop dead between two
+                        // halves of the same set.
+                        console.warn('==rntp-web: could not start the next track', err);
+                        this.onNextTrackFailed?.(err);
+                        break;
                     }
                     this.onPlaylistEnded();
                 }
@@ -56,8 +69,21 @@ export class PlaylistPlayer extends Player {
             throw new Error('playlist_exhausted');
         }
         if (this.currentIndex !== index) {
+            // PATCHED. The index is claimed BEFORE the load, and a load can
+            // fail — a VK link signed an hour ago at the end of a long show is
+            // exactly the case. Unwound, the player reported an active index
+            // whose track it had never loaded, and everything that asks it
+            // where it is got a different answer from everything that asks it
+            // what is playing.
+            const previous = this._currentIndex;
             this.currentIndex = index;
-            await this.load(track);
+            try {
+                await this.load(track);
+            }
+            catch (error) {
+                this._currentIndex = previous;
+                throw error;
+            }
         }
         if (initialPosition) {
             this.seekTo(initialPosition);
diff --git a/node_modules/react-native-track-player/lib/web/TrackPlayerModule.js b/node_modules/react-native-track-player/lib/web/TrackPlayerModule.js
index 08c7d3c..54f8833 100644
--- a/node_modules/react-native-track-player/lib/web/TrackPlayerModule.js
+++ b/node_modules/react-native-track-player/lib/web/TrackPlayerModule.js
@@ -76,6 +76,20 @@ export class TrackPlayerModule extends PlaylistPlayer {
             clearInterval(this.progressUpdateEventInterval);
         }
     }
+    /**
+     * PATCHED. The next track would not load, so the show has stopped.
+     *
+     * Emitted as an ordinary PlaybackError with a code of our own, because
+     * that is the event the app already listens to for a link that aged out —
+     * the only difference is WHICH queue entry is broken: the one after the
+     * track that just ended, not the active one.
+     */
+    onNextTrackFailed = (error) => {
+        this.emitter.emit(Event.PlaybackError, {
+            code: 'next_track_load_failed',
+            message: error?.message ?? 'the next track could not be loaded',
+        });
+    };
     async onTrackEnded() {
         const position = this.element.currentTime;
         await super.onTrackEnded();
