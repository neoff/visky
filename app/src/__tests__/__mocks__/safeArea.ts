/** react-native-safe-area-context — a phone-shaped inset, constant. */
export const useSafeAreaInsets = () => ({top: 47, bottom: 34, left: 0, right: 0})
export const SafeAreaProvider = ({children}: {children?: unknown}) => children
